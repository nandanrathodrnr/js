const name="nandan"
const countRep=50

console.log(name +countRep + "value");
// Or we can write other type also
console.log(`hello my name is ${name} and repository count is ${countRep}`)


const gamename = new String("Nandan");

console.log(gamename[0])

console.log(gamename.length)

console.log(gamename.toUpperCase())

const oneName="  nandan  "
console.log(oneName.trimStart());

const url = "https://nandan%20rathod"

console.log(url.replace('%20','_'))

console.log(url.includes('nandan'))

console.log(url.endsWith('d'));


//split will use whenever we want to split any paragraph based on the space. You can letter change in any format. 

console.log(url.concat(gamename));
console.log(url.length);

// we can use any method by using the .


